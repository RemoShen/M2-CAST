import { store } from "../store";
import { sketchCanvas } from "../views/panels/sketchCanvas";

export const appRenderer = {
  //need to subscribe
  // toggleSystemTouch: () => {
  //     dragging.set({ enable: !store.getState().systemTouch });
  //     zoomChart.set({ enable: !store.getState().systemTouch });
  // },
  // renderSketchCanvas: () => {
  //     store.getState().sketching.isSketching ? sketchCanvas.startDrawing(store.getState().sketching.startCoord) : sketchCanvas.hideCanvas();
  // }
};
